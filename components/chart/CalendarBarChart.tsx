import { BarChartData } from "@/components/chart/types";

interface CalendarBarChartProps {
  barChartData: BarChartData[];
  initialDateRange: {
    start: string;
    end: string;
  };
}

/* TODO */

export function CalendarBarChart({ barChartData, initialDateRange }: CalendarBarChartProps) {

  return (
    <></>
  );
}